import Terminal from "@/modules/terminal/components/Terminal";

export const metadata = {
  title: "Terminal | Zaeeon",
  description: "Interactive terminal portfolio mode.",
};

export default function TerminalPage() {
  return <Terminal />;
}